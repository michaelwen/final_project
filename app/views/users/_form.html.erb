<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :followers_id %><br>
    <%= f.text_field :followers_id %>
  </div>
  <div class="field">
    <%= f.label :playlists_id %><br>
    <%= f.text_field :playlists_id %>
  </div>
  <div class="field">
    <%= f.label :following_id %><br>
    <%= f.text_field :following_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
